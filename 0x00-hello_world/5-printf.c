#include <stdio.h>

/**
 * main - this function prints a string with print f
 * Description: the rpogram prints a coll statement
 * Return: 0
 * /
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
